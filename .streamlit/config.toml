[server]
port = 8501
address = "0.0.0.0"
headless = true
enableCORS = false
enableXsrfProtection = false

[browser]
serverAddress = "localhost"  # ← MUST be string, not boolean!
serverPort = 8501
gatherUsageStats = false

[logger]
level = "info"